{
  "name": "conva",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "env": "node env.js",
    "test": "mocha",
    "client-test": "npm run test --prefix client",
    "client-test:auto": "npm run test:auto --prefix client",
    "client-install": "npm install --prefix client",
    "client-ci": "npm ci --prefix client",
    "client-build": "npm run build --prefix client",
    "client-deploy": "npm run deploy --prefix client",
    "heroku-prebuild": "npm install pm2 -g",
    "start": "pm2-runtime start ecosystem.config.js --env production",
    "server": "nodemon server",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "full-test": "set CI=true && npm test && npm run client-build && npm run unbuild --prefix client && npm run client-test:auto",
    "commit": "npm run full-test && git add . && git status && git cz"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ranjanistic/conva.git"
  },
  "author": "",
  "contributors": [
    "Priyanshu Ranjan",
    "Rajat Shrivastav"
  ],
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ranjanistic/conva/issues"
  },
  "homepage": "https://github.com/ranjanistic/conva#readme",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^4.4.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.6.4",
    "socket.io": "^3.1.0"
  },
  "devDependencies": {
    "commitizen": "^4.2.3",
    "cz-conventional-changelog": "^3.2.0",
    "concurrently": "^5.3.0",
    "es6-promisify": "^6.1.1",
    "mocha": "^8.2.1",
    "nodemon": "^2.0.7",
    "pm2": "^4.5.4"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
